"""
URL Configuration for auth_core
"""
from django.urls import path
from . import views   # import views from app

app_name = "auth_core"

urlpatterns = [
    # add url patterns for the auth_core app here

    # Examples:
    # path('', views.home, name='home'),
    # path('thing', views.thing, name='thing'),
]
